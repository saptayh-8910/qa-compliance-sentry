[pytest]
testpaths = tests
pythonpath = .
markers =
    smoke: quick critical-path checks
    regression: broader coverage
    api: API validation tests
    db: database consistency tests
    external: requires access to a public network service
    ai: exercises an external AI model and may incur API cost
    chatbot: deterministic end-to-end QA assistant conversation
addopts = -ra --strict-markers
